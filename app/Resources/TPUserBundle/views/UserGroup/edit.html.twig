{% extends 'TPUserBundle::base.html.twig' %}

{% block body -%}
    <h1>Editar Grupos de usuarios</h1>

    <form action="{{ path('tp_usergroup_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Editar</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tp_usergroup') }}">
            Volver al listado
        </a>
    </li>
    <li>
        <form action="{{ path('tp_usergroup_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Borrar</button>
        </form>
    </li>
</ul>
{% endblock %}
