{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Cursos Virtuales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre curso</th>
                <td>{{ entity.nombreCurso }}</td>
            </tr>
            <tr>
                <th>Descripción curso</th>
                <td>{{ entity.descripcionCurso }}</td>
            </tr>
            <tr>
                <th>Url audio</th>
                <td><a href="{{ entity.urlAudio }}" target="_blank">{{ entity.urlAudio }}</td>
            </tr>
            <tr>
                <th>Url curso</th>
                <td><a href="{{ entity.urlCurso }}" target="_blank">{{ entity.urlCurso }}</a></td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
        </tbody>
    </table>
            <hr>
        <a href="{{ path('cursosvirtuales') }}" class="btn btn-success"><span class="glyphicon glyphicon-arrow-left"></span>
            Volver al listado
        </a>
        <a href="{{ path('cursosvirtuales_edit', { 'id': entity.id }) }}" class="btn btn-warning"><span class="glyphicon glyphicon-edit"></span>
            Editar
        </a>
    {{ form(delete_form) }}
{% endblock %}
