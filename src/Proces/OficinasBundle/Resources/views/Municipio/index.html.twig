{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Municipio list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Nombremunicipio</th>
                <th>Tipomunicipio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('municipio_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.nombreMunicipio }}</td>
                <td>{{ entity.tipoMunicipio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('municipio_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('municipio_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('municipio_new') }}">
                Crear nuevo
            </a>
        </li>
    </ul>
    {% endblock %}
