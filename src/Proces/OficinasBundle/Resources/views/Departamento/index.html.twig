{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Listado Departamento</h1>

    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Nombredepartamento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('departamento_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.nombreDepartamento }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('departamento_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('departamento_edit', { 'id': entity.id }) }}">editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('departamento_new') }}">
                Crear nuevo
            </a>
        </li>
    </ul>
    {% endblock %}
