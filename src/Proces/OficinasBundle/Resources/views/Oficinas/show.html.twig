{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Oficinas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>No.</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombreoficina</th>
                <td>{{ entity.nombreOficina }}</td>
            </tr>
            <tr>
                <th>Direccionoficina</th>
                <td>{{ entity.direccionOficina }}</td>
            </tr>
            <tr>
                <th>Descripcionoficina</th>
                <td>{{ entity.descripcionOficina }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('oficinas_admin') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('oficinas_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
