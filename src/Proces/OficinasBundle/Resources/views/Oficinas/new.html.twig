{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Crear Oficinas</h1>

    {{ form(form) }}
    <hr>
    <a href="{{ path('oficinas_admin') }}" class="btn btn-success"><span class="glyphicon glyphicon-arrow-left"></span>
            
           Volver al listado
        </a>

{% endblock %}
