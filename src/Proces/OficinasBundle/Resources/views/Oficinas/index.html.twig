{% extends 'ThemesBundle::backend.html.twig' %}
{% block body -%}
    <h1>Oficinas list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombreoficina</th>
                <th>Direccionoficina</th>
                <th>Descripcionoficina</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('oficinas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreOficina }}</td>
                <td>{{ entity.direccionOficina }}</td>
                <td>{{ entity.descripcionOficina }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('oficinas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('oficinas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('oficinas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
