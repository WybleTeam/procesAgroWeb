{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>PasosOferta edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
         <a href="{{ path('ofertasinstitucionales_show', { 'id': oferta }) }}">Volver</a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
