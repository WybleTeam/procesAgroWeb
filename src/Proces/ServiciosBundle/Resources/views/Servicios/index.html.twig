{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Listado de Servicios</h1>

    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Tituloservicio</th>
                <th>Descripcionservicio</th>
                <th>Urlaudioservicio</th>
                <th>Urlservicio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('servicios_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.tituloServicio }}</td>
                <td>{{ entity.descripcionServicio }}</td>
                <td>{{ entity.urlAudioServicio }}</td>
                <td>{{ entity.urlServicio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('servicios_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('servicios_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('servicios_new') }}">
                Crear nuevo
            </a>
        </li>
    </ul>
    {% endblock %}
