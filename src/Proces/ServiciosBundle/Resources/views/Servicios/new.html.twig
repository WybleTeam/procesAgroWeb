{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
           <div class="panel panel-default">
               <div class="panel-heading"><h3 class="panel-title">Creación de Servicios</h3></div>
                   <div class="panel-body">

    {{ form(form) }}
    <hr>
        <a href="{{ path('servicios') }}" class="btn btn-success"><span class="glyphicon glyphicon-arrow-left"></span>
            Volver al Listado
        </a>
    
                   </div>
           </div>
{% endblock %}
