{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
 <div class="panel panel-default">
     <div class="panel-heading"><h3 class="panel-title">Listado Motivo Identificación</h3></div>
          <div class="panel-body">
               <a href="{{ path('motivoidentificacion_new') }}" class="btn btn-info"><span class="glyphicon glyphicon-ok"></span>
                Crear nuevo
            </a>
        
    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Nombre motivo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('motivoidentificacion_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.nombreMotivo }}</td>
                <td>

                        <a href="{{ path('motivoidentificacion_show', { 'id': entity.id }) }}" class="btn btn-success btn-sm"><span class="glyphicon glyphicon-search"></span></a>
                        <a href="{{ path('motivoidentificacion_edit', { 'id': entity.id }) }}" class="btn btn-warning btn-sm"><span class="glyphicon glyphicon-edit"></span></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

          </div>
 </div>
    {% endblock %}
