{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Listado Motivo Identificación</h1>

    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Nombre motivo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('motivoidentificacion_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.nombreMotivo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('motivoidentificacion_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('motivoidentificacion_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('motivoidentificacion_new') }}">
                Crear nuevo
            </a>
        </li>
    </ul>
    {% endblock %}
