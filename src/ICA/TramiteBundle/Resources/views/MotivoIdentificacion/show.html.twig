{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Motivo Identificacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre motivo</th>
                <td>{{ entity.nombreMotivo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('motivoidentificacion') }}">
            Volver al Listado
        </a>
    </li>
    <li>
        <a href="{{ path('motivoidentificacion_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
