{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Especie Animal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre especie</th>
                <td>{{ entity.nombreEspecie }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('especieanimal') }}">
            Volver al listado
        </a>
    </li>
    <li>
        <a href="{{ path('especieanimal_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
