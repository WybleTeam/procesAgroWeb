{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Listado Especie Animal</h1>

    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Nombreespecie</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('especieanimal_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.nombreEspecie }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('especieanimal_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('especieanimal_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('especieanimal_new') }}">
                Crear nueva
            </a>
        </li>
    </ul>
    {% endblock %}
