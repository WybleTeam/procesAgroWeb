{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Listado Especie Rango Solicitud</h1>

    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Cantidad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('especierangosolicitud_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.cantidad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('especierangosolicitud_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('especierangosolicitud_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('especierangosolicitud_new') }}">
                Crear nueva
            </a>
        </li>
    </ul>
    {% endblock %}
