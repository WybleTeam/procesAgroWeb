{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
    <h1>Especie Rango Solicitud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ entity.cantidad }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('especierangosolicitud') }}">
            Volver al listado
        </a>
    </li>
    <li>
        <a href="{{ path('especierangosolicitud_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
