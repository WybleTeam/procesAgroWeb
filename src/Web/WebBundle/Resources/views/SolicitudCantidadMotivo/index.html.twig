{% extends 'ThemesBundle::backend.html.twig' %}

{% block body -%}
     <div class="panel panel-default">
            
            
       <div class="panel-heading"><h3 class="panel-title">Listado Solicitud Cantidad Motivo</h3>
<div class="panel-body"> 
    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>Cantidad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('solicitudcantidadmotivo_show', { 'id': entity.id }) }}">{{ loop.index }}</a></td>
                <td>{{ entity.cantidad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('solicitudcantidadmotivo_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('solicitudcantidadmotivo_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('solicitudcantidadmotivo_new') }}">
                Crear nueva
            </a>
        </li>
    </ul>
</div>
       </div>
    {% endblock %}
